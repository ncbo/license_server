# Valid LicensePurpose fixtures (the original scaffold used a non-existent `order`
# column and duplicate values). Columns: name, sort_order (unique, not null).

research_individual:
  name: "Non-Profit Research - Individual"
  sort_order: 1

education:
  name: "Education"
  sort_order: 4
